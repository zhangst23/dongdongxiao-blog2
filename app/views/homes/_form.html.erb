<%= simple_form_for(@home) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :index %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
